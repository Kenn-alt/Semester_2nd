# Area of a Parallelogram
import math

base = int(input('Enter the length of the base: '))
h = int(input("Enter the height: "))

area = float(base * h) # I used the 'float()' function, because the result was 'floating-point number' 30.0

print("The area:", area)